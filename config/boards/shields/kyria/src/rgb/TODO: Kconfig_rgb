menu "Extra RGB config"

config RGB_REFRESH_MS
	int "refresh rate"
	default 25

config RGB_TRANSITION_DURATION_START
	int "duration of transition between colors or effects"
	default 100

config RGB_TRANSITION_DURATION_STEP
	int "duration of transition between colors or effects"
	default 50
